# Optional external image-host integration.
IMGBB_API_KEY=

# Host directory exposed to the Notes service. Relative paths resolve from the repo.
VSTART2_NOTES_ROOT=./data/notes

# Optional native bridge overrides.
VSTART_MAILCTL_PATH=
VSTART_LIGHT_CLI_PATH=
